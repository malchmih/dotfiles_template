- clean: ['~']
- link:
    ~/.vimrc: vimrc
    ~/.config/fish/config.fish:
      create: true
      path: config.fish
    ~/.ideavimrc: ideavimrc
- shell:
  -
    - git submodule update --init --recursive
    - Installing/updating submodules
